/** Property entity (matches API). */
export interface Property {
  id: number;
  name: string;
  address: string;
  property_type: string;
  status: string;
  created_at: string;
  updated_at: string;
}
